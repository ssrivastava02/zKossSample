package myzkProto1.model;
import org.zkoss.zk.ui.select.SelectorComposer;
import org.zkoss.zk.ui.select.annotation.Wire;
import org.zkoss.zk.ui.select.annotation.Listen;
import org.zkoss.zul.Window;
import org.zkoss.zul.Radiogroup;
import org.zkoss.zul.Row;
import org.zkoss.zul.Rows;
import org.zkoss.zul.Messagebox;
import org.zkoss.zul.Radio;
import org.zkoss.zul.Button;
import org.zkoss.zul.Textbox;
import org.zkoss.zul.Label;
import org.zkoss.zul.Grid;

public class SecondPage extends SelectorComposer<Window> {


	/*
	 * @Wire private Radiogroup fruitsRadiogroup;
	 * 
	 * @Wire private Button printButton;
	 * 
	 * @Wire private Textbox inputUserName;
	 * 
	 * @Wire private Grid displayUser;
	 * 
	 * @Listen("onClick = button#printButton") public void printSelectedFruit() {
	 * Radio selectedRadio = fruitsRadiogroup.getSelectedItem();
	 * 
	 * if(selectedRadio != null) { //Extraction of values String selectedFruit =
	 * selectedRadio.getLabel(); String userName = inputUserName.getValue();
	 * 
	 * //Conversion to label for later appending Label userLable = new
	 * Label(userName); Label fruitLable = new Label(selectedFruit);
	 * 
	 * 
	 * //Creation of a new row and add labels to it Row newRow = new Row();
	 * newRow.appendChild(userLable); newRow.appendChild(fruitLable);
	 * 
	 * Rows rows= displayUser.getRows(); rows.appendChild(newRow);
	 * 
	 * inputUserName.setValue(""); fruitsRadiogroup.setSelectedIndex(-1);
	 * 
	 * //Messagebox.show("Selected Fruit:" + selectedFruit, "Information",
	 * Messagebox.OK, Messagebox.EXCLAMATION); } else {
	 * Messagebox.show("Please select a fruit !" , "Warning", Messagebox.OK,
	 * Messagebox.EXCLAMATION); } }
	 * 
	 * @Listen("onMouseOver = button#printButton") public void changeColor() {
	 * printButton.
	 * setStyle("background-color: #D04848; color: #ffffff; height=30%; width=30%; style= white-space: normal; margin-left: 30%;"
	 * ); }
	 * 
	 * @Listen("onMouseOut = button#printButton") public void revertColor() {
	 * printButton.
	 * setStyle("background-color: #0064ed; color: #ffffff; height=30%; width=30%; style= white-space: normal; margin-left: 30%;"
	 * ); }
	 */

}
